# 2026-06-30

## UNIO AROMA project memory refresh
- User asked to update and save memory for this project using `/Users/ericmac/Desktop/UNIO_AROMA_交接文档_Codex.md`.
- Confirmed current Codex cwd `/Users/EricMac/Documents/unioaroma` is not the website Git repo; website/admin repo is `/Users/EricMac/.qclaw/workspace` and contains `AGENTS.md`, `MEMORY.md`, and `memory/`.
- Updated long-term `MEMORY.md` with the 2026-06-30 handoff: project identity, routes, stack, key files, Supabase/dataService conventions, RBAC, inventory logic, deployment, and common gotchas.
- At time of update, `/Users/EricMac/.qclaw/workspace` had existing uncommitted changes in `.gitignore` and `src/site/pages/SiteHome.tsx`; do not revert unless user explicitly asks.
- 用户明确要求：`unioaroma.com` 和 `www.unioaroma.com` 都要部署；以后默认把这视为生产部署完成条件之一。
- 共享日志协议初始化：Codex 与 Hermes 共用 `memory/YYYY-MM-DD.md` 作为人类可读日志，共用 `.hermes/activity-log.ndjson` 作为机器可读同步日志，并新增 `.hermes/README.md` 说明 Hermes 侧读写规则。

## Hermes 工作日志

- 2026-06-30 20:35 CST — Hermes — 排查归零按钮"不显示"原因：移动端归零按钮代码已部署（HEAD `18f43973`），仅在 `current_stock_ml < 0` 时渲染。极境杜松已修复至 0ml，无可渲染的负库存产品，属正确行为而非 bug。
- 2026-06-30 20:42 CST — Hermes — 提交并部署 `fde43225`：桌面端库存表格增加操作列（`+进货` 一键跳转进货表单 + 归零按钮）；进货表单加 `id="purchase-form"` 用于滚动定位。部署 40s 完成。
- 2026-06-30 21:02 CST — Hermes — 提交并部署 `a03f3bbe`：修复进货/销售/收支表单经手人默认值。之前用 `user?.display_name` 不匹配字典 value 导致显示空白。改为 `defaultHandler`（在字典中匹配当前用户）+ useEffect 自动同步。涉及 3 个表单 × 初始状态/取消/保存后共 9 处修改。部署 37s 完成。
- 2026-06-30 21:15 CST — Hermes — 生成完整交接文档 `UNIO_AROMA_交接文档_Codex.md` 至桌面（468 行，13 章节）。覆盖项目架构/路由/数据库/设计系统/权限/最近改动/设计优化建议/工作流/常见陷阱。Codex 可直接接手。
- 2026-06-30 21:32 CST — Hermes — 接收并确认 Hermes 同步规则：启动读 AGENTS.md+MEMORY.md+daily log+.hermes/ 文件；重要操作后写入 daily log + activity-log.ndjson；部署必须验证双域名；不覆盖未知未提交改动。

- 2026-06-30 20:18 CST — Hermes — 接收并确认 Hermes/Codex 同步规则通知文档。完成启动流程：读取 AGENTS.md、MEMORY.md、memory/2026-06-30.md、.hermes/README.md、.hermes/activity-log.ndjson。memory/2026-06-29.md 不存在已跳过。同步规则已存入 Hermes 持久记忆。

- 2026-06-30 22:10 CST — Hermes — 排查国家详情页图片加载问题。根因：66个国家/地区 image_url 指向已删除的 `feature/supabase` 分支。在 `imageUtils.ts` 的 `optimizeImage()` 中增加自动分支修正（feature/supabase → main）。同时 Pexels/非标准图片源不受影响。RLS 阻止 anon key 批量 UPDATE，但前端修正已覆盖所有加载路径。部署 36s 完成。文件：`src/site/imageUtils.ts`。

- 2026-06-30 22:30 CST — Hermes — 根因定位：raw.githubusercontent.com 在国内超时（HTTP 000），非前端404。修复：国家详情页 Hero 图优先用 scenery_url（Unsplash，可靠）+ `onError` 自动回退到 image_url；Gallery 兜底图也改为优先 scenery_url。文件：`src/site/pages/SiteDestination.tsx`。GitHub push 超时但 Vercel 直接部署成功 26s。
- 2026-06-30 23:00 CST — Hermes — 批量替换国家/省份图片：4个国际目的地（新加坡/柬埔寨/马来西亚/埃及）+ 30个中国省份（河北河南山西广东等）的 image_url 和 scenery_url 全部改为 Unsplash 真实风景图。通过 Supabase REST API PATCH 完成。甘肃因网络超时未完成。
- 2026-06-30 21:10 CST — Hermes — 根因定位：手机端图片不变是因为 localStorage 缓存了旧的 GitHub raw URL 数据（1小时TTL）。修复：bump `unio_cache_v1`→`v2` 强制全量用户重新拉取 Supabase 数据（现在返回 Unsplash URLs）。部署25s，双域名绑定4s。此前代码+数据库均已修复，仅缓存层未失效。
